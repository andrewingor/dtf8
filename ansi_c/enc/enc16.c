//enc16	 Encode 16 bits to 3 octets of DTF-8
//(c)2018 by goo.gl/FFF1Up

#include "dtf8enc.h"

//
void enc16(
) {
/*
	printf("input\n");
	for (i = 0; i < 8; i++
	) {
		printf( "%c%c%c%c %c%c%c%c\n", 
			(input[i] & 0x80 ? '1':'0'), \
			(input[i] & 0x40 ? '1':'0'), \
			(input[i] & 0x20 ? '1':'0'), \
			(input[i] & 0x10 ? '1':'0'), \
			(input[i] & 0x08 ? '1':'0'), \
			(input[i] & 0x04 ? '1':'0'), \
			(input[i] & 0x02 ? '1':'0'), \
			(input[i] & 0x01 ? '1':'0')
		);
	}
	printf("output\n");
	for (i = 0; i < 11; i++
	) {
		printf( "%c%c%c%c %c%c%c%c\n", 
			(out[i] & 0x80 ? '1':'0'), \
			(out[i] & 0x40 ? '1':'0'), \
			(out[i] & 0x20 ? '1':'0'), \
			(out[i] & 0x10 ? '1':'0'), \
			(out[i] & 0x08 ? '1':'0'), \
			(out[i] & 0x04 ? '1':'0'), \
			(out[i] & 0x02 ? '1':'0'), \
			(out[i] & 0x01 ? '1':'0')
		);
	}
*/
}
